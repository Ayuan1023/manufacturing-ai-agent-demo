# Optional: leave empty to use deterministic Mock mode.
OPENAI_API_KEY=
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Set to 1 only after installing the optional rag dependencies.
ENABLE_VECTOR_RAG=0
